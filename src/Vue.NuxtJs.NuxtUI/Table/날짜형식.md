
##
```
const formatDate = ({ getValue }: any) => {
  const value = getValue()
  if (!value) return '-'
  return new Date(value).toLocaleString('en-US', {
    day: 'numeric', month: 'short', hour: '2-digit', minute: '2-digit', hour12: false
  })
}

...

{ 
  accessorKey: 'createdAt', 
  header: '작성일'
  ,cell: formatDate
}
```
```
{ 
  accessorKey: 'createdAt', 
  header: '작성일'
  ,cell: ({ getValue }: { getValue: any }) => {
    const value = getValue() // 자동으로 'createdAt'의 값을 가져옴
    if (!value) return '-'

    return new Date(value as string).toLocaleString('en-US', {
      day: 'numeric',
      month: 'short',
      hour: '2-digit',
      minute: '2-digit',
      hour12: false
    })
  }
}
```
```
{ 
  accessorKey: 'createdAt', 
  header: '작성일'
  ,cell: ({ row }: { row: any }) => {
    return new Date(row.getValue('createdAt')).toLocaleString('en-US', {
      day: 'numeric',
      month: 'short',
      hour: '2-digit',
      minute: '2-digit',
      hour12: false
    })
  }
  }
```
```
{ 
  accessorKey: 'createdAt', 
  header: '작성일',
  cell: ({ row }: { row: any }) => {
    return new Date(row.getValue('createdAt')).toLocaleString('en-US', {
      day: 'numeric',
      month: 'short',
      hour: '2-digit',
      minute: '2-digit',
      hour12: false
    })
  }
  }
```