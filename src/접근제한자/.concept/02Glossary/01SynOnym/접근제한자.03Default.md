- 접근제한자.03Default
- Default
- ( )
- `~` (default) -> 클래스oㆍ패키지o
- `~` (default)
- (default)  다른패키지에서 상속x -패키지까지만 이기에.
- 보통 코드를 짤 때 "아무것도 안 적으면(default)" 모든 곳에서 쓸 수 있을 거라 오해하기 쉬운데, 실제로는 같은 패키지 안에서만 볼 수 있다는 점을 꼭 기억하세요
- default	기본값 (아무것도 안 쓸 때)	동일 패키지 내에서만 접근 가능

