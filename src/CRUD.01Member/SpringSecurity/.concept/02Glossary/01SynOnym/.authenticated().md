- .authenticated()
- `.authenticated()`
- `.authenticated()`.출입통제가이드라인
- ．authenticated（）
- **.authenticated()**라는 설정은 스프링 시큐리티 성벽의 **'출입 통제 가이드라인'**이라고 이해하시면 됩니다. 
- 수학적으로 표현하자면 **"이 요청을 처리하기 위한 조건은 `Authentication` 객체가 존재해야 함(Not Null)"**이라는 뜻입니다.
