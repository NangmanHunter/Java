
## 생성자참조
```java
.map(GrantedAuthority::getAuthority)
```
```java
.map((GrantedAuthority auth) -> auth.getAuthority())
```
```java
new Function<GrantedAuthority, String>() {
    @Override
    public String apply(GrantedAuthority auth) {
        return auth.getAuthority();
    }
}
```
