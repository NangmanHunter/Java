- "이게 화면에 나오는 값인가?"를 자문해 보세요.👉**NO (내부 계산용):** 일반 변수를 쓰세요.
- "이게 화면에 나오는 값인가?"를 자문해 보세요.👉**YES:** 무조건 `ref` (또는 `reactive`)를 쓰세요.
- `반응형변수`
- `ref`
- `ref`.화면넣는것까지 한방적으로 만든개념
- `ref`.화면넣는것까지 한방적으로 만든변수개념
- `ref`.화면넣는것까지 한방적으로 만든변수개념-const에다가 ()안쪽값을 넣어버려 자유롭게 바로튀어나오게 만들어버린개념.
- `ref`.화면넣는것까지 한방적으로 만든변수
- `ref`.화면상 선언해버리면 바로반영되게하는 개념이다.
- `ref`.화면상 반응형이다.
- `ref`.반응형변수
- `ref`는 Vue 3(Composition API)에서 가장 핵심적인 개념으로, 수학으로 치면 **'변수(Variable)'**이지만, 그냥 변수가 아니라 **'살아 움직이는 변수'**입니다.
- 가장 근본적인 차이는 "Vue가 그 변수를 지켜보고 있느냐(감시)"와 "화면을 새로 그리느냐(반응)"에 있습니다.
- 반응형 참조
- 반응형변수
- 반응형참조
- 반응형참조변수
- 일반변수<<Ref변수-강하다
- 일반변수<<Ref변수-화면까지뚫고나온다
- 정식 명칭은 **Reactive Reference(반응형 참조)**입니다.
- 하지만 `ref`를 쓰면 Vue가 이 변수를 **감시**하기 시작합니다. 값이 바뀌는 순간, "어? 바뀌었네? 화면 다시 그려야지!" 하고 자동으로 업데이트를 해줍니다. 이걸 **반응형(Reactivity)**이라고 합니다.
- Reactive
- Reactive Reference
- Reactive Reference(반응형 참조)
- ReactiveReference
- ref
- Ref
- ref = 객체 + value + Proxy기술
- ref = 객체 + value속성 + Proxy기술
- ref = value + Proxy기술
- ref.반응형
- ref.생각해보니까 더강하다 화면까지 더끌어갈줄수있는 그개념.
- ref.자동 요소지우고ㆍ변수다시넣기
- ref.자동요소지우고다시넣기
- ref.자동요소지우고변수다시넣기
- ref.자동요소지우고ㆍ변수다시넣기
- ref.자동지우고다시넣기
- ref.지우고다시넣기 자동화
- ref.지우고다시넣기자동
- ref.지우고다시넣기자동화
- ref.화면까지끌어간다
- ref.화면상에서만
- ref.화면상에서만 바로반영
- ref.화면상에서만 재랜더링 알아서
- ref.화면상에서만 재랜더링 알아서-단순선언만으로 알아서 재랜더링되는 딱그것.
- ref.화면상에서만 재랜더링 알아서-예전은 지우고+다시넣기
- ref.화면상에서만 재랜더링 알아서-예전은 지우고+다시넣기👉다시넣기가 이런부분이 좀까다로운게 매번위치를 특정지점을 특정요소 특정Element를 잡아서 진행해줘야함. 이작업이 너무에너지가듬.
- ref.화면상에서만 재랜더링 알아서-지금은 선언만으로 걍끝.
- ref.화면상에서만 재랜더링 알아서-특정지점 잡아줄필요도없고.
- ref.화면상에서만 재랜더링 알아서-특정지점 잡아줄필요도없고.특정지점에서 지우고다시넣기 이런것도 할필요도없게되는
- ref.화면상에서만 재랜더링 알아서-한마디로 지우고넣기 이거를 전역성으로 위로추상성으로 한방에 싹해버린 딱그것.
- ref.화면상에서만.백엔드는 결국 통신해야함
- ref.화면상에서만바로반영
- ref.화면상에서만바로반영-예전같았으면 해당부분지우고.그위에 다시얹는 그런형태.
- ref.화면상에서만바로반영-예전같았으면 해당부분지우고.그위에 다시얹는 그런형태.-이부분을 알아서해주게끔 만든격
- ref.화면상에서만바로반영.굳이한번더 재호출안해도됨.
- ref.화면상에서만바로반영됨
- ref.화면상에서만바로반영보여짐
- ref.ref변수
- ref변수
- Ref변수
- Ref변수>>일반변수
- Ref변수>>일반변수-변한것바로반영위해
- Ref변수>>일반변수-변한것바로반영할때
- Ref변수>>일반변수-변한것반영할때
- ref선언.`변수.value`
- `ref()` 괄호 안에 넣는 값은 **초기값**입니다.
- `ref(0)`: 숫자로 시작하는 반응형 변수
- `ref('안녕')`: 문자열로 시작하는 반응형 변수
- `ref([])`: **빈 리스트(Array)**로 시작하는 반응형 변수 (게시판 목록 등을 담을 때 쓰겠죠?)
- `ref`
- `ref`.좀중요한게 화면상 이부분한정이라는것.-백엔드까지 절대ㄴㄴ. 별도로 fetch axios 이런거써줘야된다ㄱㄱ.
- ref.요소찾아ㆍ집어넣는다 대체
- ref.초기값갖고와서ㆍ요소찾아ㆍ집어넣는다 대체
- ref.초기값갖고와서ㆍ요소찾아ㆍ집어넣는다 대체-요소찾는것에서도 이게큰게 `document.getElementById`이런거없이 걍 `@click`바로 꽂을수있음. 이런사소한게큰듯.
- ref.초기값갖고와서ㆍ요소찾아ㆍ집어넣는다 대체-철저히 더 Python적으로 진행되는 그런느낌.-대신 웹상에서 가는거라 웹적요소 이런특이적성질들이있음. 이런걸 싹최적화해주는게 더맞는듯ㄱㄱ.
- ref.초기값갖고와서ㆍ요소찾아ㆍ집어넣는다 대체-이런사소한게 좀큰듯.
- ref.초기값갖고와서ㆍ요소찾아ㆍ집어넣는다 대체-심지어 초기값갖고오는것도 싹다 자동화해줘버림.
- ref.요소찾아ㆍ집어넣는다 자동화
- ref.요소찾아ㆍ집어넣는다자동화
- ref.요소찾아(document.get...)ㆍ집어넣는다(.innerText) 대체
- ref.요소찾아(document.get...)ㆍ집어넣는다(.innerText) 자동화