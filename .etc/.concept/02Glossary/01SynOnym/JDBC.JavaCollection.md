- JavaCollection
- JDBC이전에 JavaCollection
- "자바 컬렉션(Map, List)으로 간다"는 말은 데이터베이스(H2, MySQL 등)를 아예 설치하지도, 쓰지도 않겠다는 뜻입니다.
- 데이터를 '파일'이나 'DB 서버'에 저장하는 대신, 그냥 자바 프로그램이 실행 중인 메모리(RAM) 공간에 변수 형태로 담아두는 것이죠.
- 문제는 여기까지할거냐.-ㄴㄴ다. 많이했었고. 너무 무용적인형태가 강하다.
- 더실용적형태로 더가줄것. 그위해 더쌓는식으로 더가줄것ㄱㄱ.
- DB 설정 파일(application.yml)도 필요 없고, SQL도 안 씁니다.
- 코드는 데이터베이스(DB) 없이 자바의 메모리만을 활용해 데이터를 저장하는 가장 전형적이고 깔끔한 방식입니다. 흔히 '인메모리 저장소 가짜 구현체(Fake Repository)'라고도 부릅니다.