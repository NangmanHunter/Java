- "클래스로부터 객체를 만드는 과정을 인스턴스화(Instantiation)라고 하며, 그 결과물이 바로 인스턴스입니다."
- `Instance`
- 객체
- 객체.램할당
- 객체(Instance)
- 객체를 만드는과정
- 객체생성
- 결론부터 말씀드리면 실무에서는 '객체'와 '인스턴스'를 거의 같은 뜻으로 혼용해서 씁니다.
- 생성자필요
- 엄밀히 Instance
- 우리가쓰는 객체
- 인스턴스
- 인스턴스 (Instance): 지금 내 손에 들려 있는 '특정한 붕어빵' (메모리에 할당되어 실제로 존재하는 것)
- 인스턴스: new 키워드를 통해 Heap 메모리에 실제로 자리를 잡았을 때, "이 녀석은 Student 클래스의 인스턴스다"라고 부릅니다.
- 인스턴스.객체.램할당
- 인스턴스.객체만드는과정
- 인스턴스.객체생성
- 인스턴스.객체생성과정
- 인스턴스(Instance)
- 인스턴스생성
- 인스턴스생성.`클래스명 변수명 = new 생성자;`
- 인스턴스생성.`클래스명 변수명 = new 생성자`
- 인스턴스생성.`클래스명 변수명 = new 생성자명();`
- 인스턴스생성.`클래스명 변수명 = new 클래스명();`
- 인스턴스생성.`클래스명 변수명 = new 클래스명();`.`클래스명 변수명 = new 생성자명();`
- 인스턴스생성.생성자필요
- 클래스에 정의된내용대로 new연산자를통해 메모리영역에생성된것
- Heap영역할당
- Instance
- Instance : 가상객체ㆍ컴퓨터객체
- Instance : 컴퓨터객체
- Instance.객체<<인스턴스
- Instance.객체<<인스턴스-표현
- st = new Student(); // 'st'는 이제 Student 클래스의 '인스턴스' (Heap 메모리에 올라감)
