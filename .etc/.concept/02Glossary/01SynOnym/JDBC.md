- JDBC
- `JDBC`
- JDBC의 역할 (왜 꼭 필요한가?)
- 자바에서 DB에 접근하려면 반드시 거쳐야 하는 표준 규격입니다.
- 연결: DB에 접속합니다.
- 전달: 작성한 SQL을 DB로 보냅니다.
- 수신: DB가 준 결과(데이터)를 자바 객체로 받아옵니다.
- JDBC 기반 조회 (SQL 필요): SELECT * FROM USER WHERE ID = 1