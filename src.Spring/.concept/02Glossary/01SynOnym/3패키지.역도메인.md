- Reverse Domain Name
- ReverseDomainName
- ReverseDomain
- DomainReverse
- 3패키지.DomainReverse
- 3패키지.역도메인
- 3패키지.도메인역순
- 3Package.DomainReverse
- 3Package.ReverseDomain
- ThreePackage.ReverseDomain
- `3Package.ReverseDomain`
- `ReverseDomainName`
- A.B.C
- `A.B.C`
- `A.B.C`.`C.B.A`
- `A.B.C`.`C.B.A`(도메인)
- `A.B.C`(Spring).`C.B.A`(도메인)
- `A.B.C`(Java).`C.B.A`(도메인)
- `A.B.C`(Package).`C.B.A`(도메인)
- `A.B.C`(Package).`C.B.A`(Domain)
- `A.B.C`(📁Package).`C.B.A`(🌐Domain)
- `A.B.C`(대소).`C.B.A`(소대)
- `A.B.C`-도메인명 거꾸로ㄱㄱ.
- 자바 관습: 전 세계적인 이름 중복 방지 (도메인 뒤집기).
- 자바의 네이밍 관습 (Reverse Domain Name)
- 자바는 클래스 이름이 중복되는 것을 방지하기 위해 도메인을 거꾸로 뒤집은 형태를 패키지 이름으로 사용하는 관습이 있습니다.
- 예: `com.naver.project`, `org.apache.utils`
- 이 때문에 `src/main/java` 아래에 최소 `com`, 그 아래 `example`, 그 아래 `myapp` 처럼 최소 3단계 이상의 깊이가 기본적으로 생기게 됩니다.
- 3패키지.역도메인.스프링(Spring) 프로젝트를 처음 생성했을 때 마주하는 그 복잡한 디렉토리 구조, 충분히 당혹스러울 수 있습니다. 
- 사실 이건 스프링 자체가 강제하는 "절대 법칙"이라기보다는, 자바의 관습과 스프링 부트의 자동 설정(Auto-configuration) 메커니즘이 결합되어 만들어진 표준 가이드라인에 가깝습니다.
- 3패키지.역도메인.스프링 메커니즘: 메인 클래스 위치를 기준으로 부품들을 자동으로 찾기 위해.
- 3패키지.역도메인.질서: 코드가 섞이지 않게 역할별로 서랍을 나누는 과정.
- 3패키지.역도메인.처음에는 폴더를 계속 타고 들어가는 게 귀찮게 느껴지겠지만, 프로젝트 규모가 조금만 커져도 이 구조 덕분에 "어디에 무슨 코드가 있는지" 단번에 찾을 수 있게 된답니다. 